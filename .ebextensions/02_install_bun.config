commands:
  01_install_bun:
    command: "curl -fsSL https://bun.sh/install | bash && export PATH='$HOME/.bun/bin:$PATH'"
    ignoreErrors: false
  02_verify_bun:
    command: "source ~/.bashrc && bun --version"
    ignoreErrors: true