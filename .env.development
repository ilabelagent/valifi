# Development Environment Configuration
NODE_ENV=development

# Server Ports
PORT=3001
VITE_PORT=5000

# API Configuration
API_URL=http://localhost:3001/api
VITE_API_URL=http://localhost:3001/api

# Database (uses existing DATABASE_URL from secrets)
# DATABASE_URL is already configured in Replit secrets

# JWT (uses existing JWT_SECRET from secrets)
# JWT_SECRET is already configured in Replit secrets
